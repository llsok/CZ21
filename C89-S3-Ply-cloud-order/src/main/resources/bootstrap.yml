spring:
   application:
      name: cloud-order
   cloud:
     config:
       uri: http://localhost:9000
       #    分支
       label: main
       #   配置片名： 开发（dev），测试（test），生产（prod）
       # profile: prod
   
