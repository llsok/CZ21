server:
   # 8001,8005,8006
   port: 8001
   
spring:
   application:
      name: cloud-user
      
eureka:
   client:
      serviceUrl:
         defaultZone: http://127.0.0.1:8000/eureka

# 设定 feign.hystrix.enabled=true，那么Feign将用断路器包装所有的控制层方法
feign:
  hystrix:
    enabled: true
         